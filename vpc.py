import boto3\nimport logging\n\n# Setting up logging\nlogging.basicConfig(level=logging.INFO)\n\ndef delete_security_group(security_group_id):\n    client = boto3.client('ec2')\n    try:\n        # Attempt to delete the security group\n        client.delete_security_group(GroupId=security_group_id)\n        logging.info(f'Successfully deleted security group: {security_group_id}')\n    except client.exceptions.ClientError as e:\n        logging.error(f'Failed to delete security group {security_group_id}: {e}')\n    except Exception as e:\n        logging.error(f'An unexpected error occurred while deleting security group {security_group_id}: {e}')\n\n# Example usage\n#if __name__ == '__main__':\n#    delete_security_group('sg-12345678')